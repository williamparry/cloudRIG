{
  "name": "cloudrig",
  "version": "0.0.1",
  "description": "Stream your games on the cheap.",
  "main": "index.js",
  "scripts": {
    "postinstall": "cd ../lib && rm -rf node_modules && yarn install && yarn link && cd ../cli && yarn link cloudriglib",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "async": "^2.5.0",
    "cloudriglib": "file:../lib",
    "cowsay": "^1.2.1",
    "figlet": "^1.2.1",
    "inquirer": "^6.2.0",
    "prettyjson": "^1.1.3",
    "yargs": "^12.0.2"
  }
}
