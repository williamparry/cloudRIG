{
	"name": "cloudrig",
	"version": "0.1.0",
	"private": true,
	"scripts": {
		"dev": "concurrently --kill-others \"cd react && BROWSER=none npm run start\" \"./node_modules/.bin/electron electron\"",
		"build": "react-scripts build"
	},
	"devDependencies": {
		"cloudrigelectron": "file:./electron",
		"cloudrigreact": "file:./react",
		"concurrently": "^3.5.0"
	}
}
