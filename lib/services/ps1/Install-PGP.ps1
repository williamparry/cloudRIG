# TODO: https://www.nuget.org/packages/NetPGPLibrary/
# The idea is that the EC2 box can generate a public and private key
# The CLI then uses the public key to encrypt the user's steam username and password
# The EC2 box then decrypts and sets a steam auto login shortcut